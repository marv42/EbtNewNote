buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.6.4'
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}

ext {
    sdkVersion = 30
    buildToolsVersion = '30.0.1'
}
